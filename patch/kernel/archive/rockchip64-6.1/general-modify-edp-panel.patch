From 4b4e0e142b5b137ee21c0e479c35ebc1869cdf18 Mon Sep 17 00:00:00 2001
From: rubin <rubin@126.com>
Date: Mon, 27 May 2024 03:40:42 +0000
Subject: [PATCH] general-modify-edp-panel

---
 drivers/gpu/drm/panel/panel-edp.c | 31 +++++++++++++++++++++++++++++++
 1 file changed, 31 insertions(+)

diff --git a/drivers/gpu/drm/panel/panel-edp.c b/drivers/gpu/drm/panel/panel-edp.c
index 70feee787611..a4eb9ccc4edd 100644
--- a/drivers/gpu/drm/panel/panel-edp.c
+++ b/drivers/gpu/drm/panel/panel-edp.c
@@ -778,6 +778,7 @@ static int generic_edp_panel_probe(struct device *dev, struct panel_edp *panel)
 		ret = -EIO;
 		goto exit;
 	}
+	dev_info(dev, "get edp panel EDID=%x\n",panel_id);
 	drm_edid_decode_panel_id(panel_id, vend, &product_id);
 
 	panel->detected_panel = find_edp_panel(panel_id);
@@ -1722,6 +1723,33 @@ static const struct panel_desc starry_kr122ea0sra = {
 		.unprepare = 10 + 500,
 	},
 };
+static const struct drm_display_mode xiyun_unknownpanel_mode = {
+	.clock = 96000,
+	.hdisplay = 1600,
+	.hsync_start = 1600 + 148,
+	.hsync_end = 1600 + 148 + 80,
+	.htotal = 1600 + 148 + 80 + 180,
+	.vdisplay = 900,
+	.vsync_start = 900 + 60,
+	.vsync_end = 900 + 60 + 20,
+	.vtotal = 900 + 60 + 20 + 80,
+	.flags = DRM_MODE_FLAG_NVSYNC | DRM_MODE_FLAG_NHSYNC,
+};
+
+static const struct panel_desc xiyun_unknownpanel = {
+	.modes = &xiyun_unknownpanel_mode,
+	.num_modes = 1,
+	.size = {
+		.width = 145,
+		.height = 92,
+	},
+	.delay = {
+		/* TODO: should be hpd-absent and no-hpd should be set? */
+		.hpd_reliable = 200 + 200,
+		.enable = 200,
+		.unprepare = 200 + 500,
+	},
+};
 
 static const struct of_device_id platform_of_match[] = {
 	{
@@ -1811,6 +1839,9 @@ static const struct of_device_id platform_of_match[] = {
 	}, {
 		.compatible = "starry,kr122ea0sra",
 		.data = &starry_kr122ea0sra,
+	}, {
+		.compatible = "xiyun,unknown-panel",
+		.data = &xiyun_unknownpanel,
 	}, {
 		/* sentinel */
 	}
-- 
2.30.2

